The *PQ.h header files and testPQ.cpp correspond to this project, i.e., part B of the pairing heaps two sided project (p2a & p2b).

You can use the command "make alltests" or "make testPQ" to try out the
priority queue tester.

I implemented 4 files: sorted, binary, pairing, and testpq.
